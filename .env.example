# Copy to .env and set your values.
# Required for database and for running the game.

DATABASE_URL=postgresql://user:password@localhost:5432/word_guess

# Optional: set if using Google AI for hints (e.g. GOOGLE_API_KEY or as per google-genai docs)
